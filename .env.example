# Copy to .env and fill in real values (never commit .env)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Google OAuth (if used) — create credentials in Google Cloud Console
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
